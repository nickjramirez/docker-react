lanugage: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t nickjramirez/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nickjramirez/docker-react npm run test